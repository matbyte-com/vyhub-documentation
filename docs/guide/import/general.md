# Import from other systems

In the `Import` settings you can import data from other systems. The section is split into one tab per source system.
Currently importing data from the following systems is possible:

- [VyHub](vyhub.md) — export a full backup archive from one VyHub instance and import it into another.
- [GExtension](gextension.md)